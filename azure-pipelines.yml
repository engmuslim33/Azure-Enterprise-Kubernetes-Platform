trigger:
  branches:
    include:
      - main

variables:
  acrName: acraekplab001
  imageName: aekp-nginx
  imageTag: $(Build.BuildId)
  aksResourceGroup: RG-AKS
  aksCluster: aks-aekp-lab
  namespace: web

stages:
  - stage: Build
    displayName: Build and Push Image
    jobs:
      - job: Docker
        displayName: Docker Build
        pool:
          vmImage: ubuntu-latest
        steps:
          - script: |
              echo "Build image placeholder"
              echo "Configure Azure service connection before enabling real push."
            displayName: Placeholder build step

  - stage: Deploy
    displayName: Deploy to AKS
    dependsOn: Build
    jobs:
      - job: Kubernetes
        displayName: Deploy Manifests
        pool:
          vmImage: ubuntu-latest
        steps:
          - script: |
              echo "Deploy placeholder"
              echo "Use AzureCLI@2 with az aks get-credentials and kubectl apply."
            displayName: Placeholder deploy step

